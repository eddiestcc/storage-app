import { useState } from "react";

const FilterListItem = ({ displayTable, setDisplayTable }) => {

//     const [filteredList, setFilteredList] = useState([]);

//     const reducedList = () => {
//         const table = displayTable.filter(tableItem => {
//         return tableItem.unitType === 'FCC';
//     });
//     // WORK IN PROGRESS 
// }
//  setDisplayTable(filteredList);

    return (
        <li className="btn w-48">Reduce!</li>
    )
}

export default FilterListItem;